# 2026-08-14

## Bus App — Route Shape 真路線修復 ✅

- 用戶發現 map 路線係拉直線，唔跟實際馬路
- 原因：worker `/api/route-shape` 係空 stub（`{coordinates: []}`），前端 fallback 直線連站
- 解決：
  - 數據源：官方 `hkbus/route-waypoints`（`https://hkbus.github.io/route-waypoints/{gtfsId}-{O|I}.json`，每日 sync CSDI，真行車線）
  - mapping：download `https://data.hkbus.app/routeFareList.min.json` (8.2MB) → `build-route-shape-map.js` 抽 `company|route|bound → {gtfsId}-{O|I}.json` → `worker/route-shape-map.js`（76KB，2747 條）
  - worker `fetchRouteShape()`：查 map → fetch GeoJSON → 轉 `[lat,lng]` 座標 → dedupe consecutive duplicates（CTB 1 由 31337 → 469 點）
- 部署：`047dbfd`，live `https://bus-eta.rexmaopenclaw.workers.dev`
- 驗證：KMB 1A O = 759 點、CTB 1 I = 469 點 ✅
- 教訓：gitignore 行尾唔可以加註解（`pattern # comment` 會連 comment 一齊做 pattern）— 要分兩行
- 注意：`routeFareList.min.json` 已 gitignored（8MB temp）
- 前端唔使改 — BusMap.tsx 已識食 `data.coordinates`
