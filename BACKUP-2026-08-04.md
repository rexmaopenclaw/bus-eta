# Bus App — 進度備份 2026-08-04

## 🟢 運作中

### Server
- **Port 3001** — Express + SQLite + JWT Auth
- **Process**: `node.exe` PID 18376 (1.2GB RAM)
- **DB**: `busapp.db` (4KB, 有 users + favorites table)
- **Startup**: `cd server && node index.js`

### Cloudflare Tunnel
- **URL**: `https://nat-replacement-positive-greetings.trycloudflare.com`
- **Process**: `cloudflared.exe` (external)
- **⚠️ URL 每次 restart 會轉**
- **Binary**: `C:\Users\chma5\cloudflared.exe`

### Web App (dist/)
- Rebuilt: 2026-08-04 10:33
- `entry-ba795a43...js` (2.36MB) + `leaflet-src-...js` (149KB)
- CTB proxy `/api/proxy/ctb` 整合入 bundle

### API Endpoints
| Endpoint | Status | Notes |
|----------|--------|-------|
| `/api/health` | ✅ | |
| `/api/login` / `/api/register` | ✅ | JWT, 30d expiry |
| `/api/favorites` | ✅ | CRUD + sync |
| `/api/proxy/kmb` | ✅ | KMB static data proxy |
| `/api/proxy/ctb` | ✅ | CTB static data proxy (NEW) |
| `/api/route-fare/:company/:route` | ✅ | From route-fare.json |
| `/api/route-shape/:company/:route/:bound` | ✅ | From hkbus/route-waypoints |

### ETA API (rt.data.gov.hk)
- **CTB stop-based**: `eta/CTB/{stop_id}/{route}` ✅ 正常回傳 3 班車
- **CTB route-level**: `eta/ctb/{route}/{bound}` ❌ 回傳 empty (upstream bug)
- **KMB**: 正常

## 🔧 最近修改 (2026-08-04)

### 1. CTB CORS Fix — Server proxy
- **File**: `server/index.js` (+ 15 lines)
- 加咗 `/api/proxy/ctb` middleware，同 KMB proxy 一樣 pattern
- `src/api/ctb.ts` → `STATIC_BASE` 由 GitHub Pages 改為 `/api/proxy/ctb`

### 2. Favorites Migration Bug Fix — Company 錯配
- **File**: `src/store/favorites.ts` (migration logic)
- **File**: `app/(tabs)/index.tsx` (DashboardCard)
- **問題**：舊版 CTB 收藏冇 `company` field，migration default 做 `'KMB'`
- **Fix**：根據 `fav.id` 前綴判斷公司（`CTB-` / `NWFB-` / `KMB-`）
- DashboardCard 直接用 `fav.id` 前綴判斷，唔靠 `company` field

### 3. 其他
- 冇 login 強制限制（可以冇 account 用）
- 群組摺疊 + 拖曳排序
- 附近車站 (nearby)
- 搜尋路線 (search)
- 地圖顯示路線形狀 (BusMap)

## 📋 Pending / Known Issues

### ⚠️ CTB ETA 未確認修復
- 理論上修復咗（favorites company 錯配 + CTB proxy），但 user 回報「未有」
- 可能原因：
  - **Web**: 未 hard refresh / cache 未清
  - **Mobile APK**: 未 rebuild native app，bundle 唔包最新 code
  - **Favorites 已 sync 去 server**：server DB 存嘅 favorites 都要 fix company field

### ⏳ EAS Build
- Latest build: `9940bcfc` (in queue, 2026-08-03 22:03)
- Build profile: `preview` (APK, internal)
- Pipeline: `src/api/auth.ts` → `API_BASE` 要用 tunnel URL
- 956MB — `server/` + `dist/` 已加 `.easignore`

### ⚡ Tunnel URL 不穩定
- trycloudflare URL 每次 restart 會變
- `src/api/auth.ts` 仲用緊舊 URL: `https://monitored-clusters-computer-misc.trycloudflare.com`
- 需要 update 先可以 login

### 🔄 未做
- [ ] 確認 user 測試 CTB ETA 結果
- [ ] 如果 mobile: rebuild native app (EAS build)
- [ ] 更新 `src/api/auth.ts` `API_BASE` 到最新 tunnel URL
- [ ] 考慮持久 tunnel (Cloudflare Argo / 自訂 domain)

## 📁 檔案結構
```
documents/mike/bus-app/
├── app/                  # Expo Router pages
│   ├── (tabs)/           # Main tabs (index, search, nearby)
│   ├── route/[route].tsx # Route detail + map
│   ├── stop/[stopId].tsx # Stop ETA page
│   └── login.tsx
├── src/
│   ├── api/              # API clients (kmb.ts, ctb.ts, auth.ts)
│   ├── hooks/            # React Query hooks (useETA.ts, useCTBETA.ts)
│   ├── store/            # Zustand stores (favorites.ts, auth.ts)
│   ├── components/       # UI components
│   ├── types/            # TypeScript types
│   └── utils/            # Utilities (theme, bound)
├── server/
│   ├── index.js          # Express server
│   ├── busapp.db         # SQLite DB
│   ├── route-fare.json   # Fare data
│   └── route-shape-mapping.json
├── dist/                 # Web export (served by Express)
├── .easignore
├── app.json
├── eas.json
└── package.json
```

## 💡 Tech Stack
- **Frontend**: Expo (React Native) + Expo Router
- **State**: Zustand + AsyncStorage
- **Data Fetching**: TanStack React Query
- **Backend**: Express + better-sqlite3
- **Auth**: JWT + bcryptjs
- **Tunnel**: Cloudflare trycloudflare
- **Static Data**: winstonma GitHub Pages (MMM-HK-Transport-ETA-Data)
- **ETA API**: rt.data.gov.hk (KMB + CTB/NWFB OpenData)